/** @type {import('next').NextConfig} */
const nextConfig = {
	basePath: "/land-taxes-calculator",
	output: "export", // <=== enables static exports
	reactStrictMode: true,
};

export default nextConfig;
